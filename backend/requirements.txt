# VIS4SRD 完整依赖
# ====================
# 整合后端所有依赖，统一管理

# ============ Web 框架 ============
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
orjson==3.10.0

# ============ 数据库驱动 ============
asyncpg==0.29.0
aiomysql==0.2.0
PyMySQL==1.1.0
mysql-connector-python==8.3.0
cryptography==43.0.0

# ============ 认证 ============
PyJWT==2.8.0
bcrypt==4.1.2

# ============ 数据处理 ============
pandas==2.2.1
numpy==1.26.4
pydantic==2.6.1
pydantic-settings==2.1.0

# ============ 工具库 ============
python-dotenv==1.0.1
httpx==0.27.0
tenacity==8.2.3
jieba>=0.42.1
dirtyjson>=1.0.0

# ============ SuiAgent AI Agent ============
openai>=1.0.0
langchain-core>=0.2.0,<0.3.0

# ============ 深度学习 & 文本处理 ============
torch>=2.0.0
transformers>=4.36.0
scikit-learn>=1.4.0

# ============ 文档处理 ============
python-docx>=1.0.0
pdfplumber>=0.10.0

# ============ 系统工具（需单独安装） ============
# pdftotext: Windows: conda install -c conda-forge poppler  |  Linux: sudo apt install poppler-utils  |  Mac: brew install poppler
# pandoc: Windows: 下载安装包 https://pandoc.org/installing.html  |  Linux/Mac: sudo apt/brew install pandoc

# ============ 可选 SDK（按需取消注释安装） ============
# dashscope>=1.14.0   # 阿里云通义千问
# zhipuai>=2.1.5     # 智谱 AI
# openai>=1.12.0     # OpenAI/DeepSeek/Kimi 等
